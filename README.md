first thing to note down is, this is just a base draft for the project i'll be working on sometime in the future


this'll be a extension to make attending contests in a much more streamlined / better way from your IDE (vscode) itself

docs : (ctrl shift p to run)

`solveera.ProblemStatement(problemLink) - (creates instance of problemstatement.txt for <problem> on <platform>)`
`sovleera.SubmitFile([location][link]) - (sumbits code to <platform>)`

everything starts small!
